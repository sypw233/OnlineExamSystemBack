# Development Profile Configuration
# Use this profile during local development

# Show SQL queries in development
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Relaxed JWT secret for local development (DO NOT use in production)
jwt.secret=dev-only-secret-key-at-least-32-characters-long-for-local-testing
